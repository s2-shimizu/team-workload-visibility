{
  "name": "team-dashboard-frontend",
  "version": "1.0.0",
  "description": "チーム状況ダッシュボード フロントエンド",
  "scripts": {
    "build": "node build-script.js",
    "build:simple": "mkdir -p build && cp -r *.html css js build/",
    "validate": "node validate-files.js",
    "verify": "node verify-build.js",
    "dev": "python -m http.server 8080",
    "start": "python -m http.server 8080",
    "clean": "if exist build rmdir /s /q build && if exist dist rmdir /s /q dist",
    "prebuild": "npm run validate",
    "postbuild": "npm run verify",
    "build:full": "npm run clean && npm run build"
  },
  "devDependencies": {},
  "dependencies": {}
}